<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-07-01 | Updated: 2026-07-01 -->

# web/src/hooks/ — API Wrappers, Reducers, Polling

## OVERVIEW

All web-side data flow lives here: raw `/api` wrappers, reducer-based domain hooks, polling cadence, persisted UI preferences, and modal/dialog accessibility utilities. Components should consume these hooks instead of owning network logic.

## WHERE TO LOOK

| Task | Location | Notes |
|------|----------|-------|
| Change card API contract | `useKanbanApi.ts` | Cards, dispatch, screenshots, models, questions |
| Change board reducer/polling/queue behavior | `useKanbanBoard.ts` | 3s polling, queue reorder, complete-all |
| Change shared list-CRUD state (entries/polling/errors) | `useCrudResource.ts` | Generic reducer behind the three hooks below |
| Change scheduler data flow | `useSchedulerApi.ts`, `useScheduler.ts` | Thin wrapper over `useCrudResource` + toggle/run |
| Change Works data flow | `useWorksApi.ts`, `useWorks.ts` | `useCrudResource` for the Work list (gated on the `works` tab **or** the Board tab's Timeline view — the Timeline reads the same Work list) + a continuously-polled Inbox for the tab badge; assign helpers (create+link) shared with the assign-all modal |
| Change Work **detail** data flow (session titles, card counts, wiki docs) | `useWorkSessions.ts`, `fetchWorkSessions` in `useWorksApi.ts` | Its own `GET /api/works/:id/sessions`, **not** part of the `useWorks` list poll: completing a Work archives every card under it, so a rollup over the board list reports `카드 0 · done 0` for exactly the Works being reviewed. It does not poll — pass the Work's `updatedAt` as `revision` and it refetches on every event that can change the answer (link, unlink, role change, completion sweep), keeping what is on screen until the response lands. A response for a Work the user already closed is dropped via `requestRef`, same guard as `useTimeline`. `WorkDetailDialog` still treats `work.sessionLinks` as authoritative for *which* rows exist so an optimistic unlink is instant; the hook only supplies each row's numbers, and a failed read falls back to board cards visibly |
| Read a card or a session that may be archived | `useKanbanApi.ts` | `fetchCard(id, { includeArchived: true })` and `fetchSessionCards(sessionId)` are what make deep links into *finished* work resolve — a Timeline day cell, a completed Work's `대화`, the Works Inbox "대화 보기". Those cards are off the board by design, so the plain reads 404/return nothing. `App.handleOpenCardById` must never reject: a rejected `void` call used to leave the click silently dead, so it reports through `showError` instead. A card opened from the archive is tracked (`selectedCardArchived`) so the 3s board poll does not read "not on the board" as "deleted" and close the dialog |
| Change Timeline data flow | `useTimeline.ts`, `fetchTimeline` in `useWorksApi.ts` | Its own windowed endpoint, **not** the Works poll: the grid draws cards that have long since been archived off the board, and `from`/`to` are what keep that read bounded — so both are required and there is no unwindowed variant. 10s polling like the other non-board views. `loading` only covers the *first* load of a window (a poll must not blank the grid), and a response from the window the user just left is discarded via `requestRef`, so paging fast never lands the wrong grid. The response deliberately carries no Work grouping — session → Work mapping happens in `timelineRows.ts` off the `useWorks` list, so an optimistic assignment re-groups immediately instead of waiting for a refetch. The `UiAlert` this hook builds carries the **raw** route/network message; `TimelineView` runs it through the pure `describeTimelineError` before rendering, because the bare text (`Not found`) over a fully drawn, session-free grid read as "nothing ran this week" |
| Change multi-session assignment | `useWorks.ts` | `assignSessionsToWork` sends **one** `POST /api/works/:id/sessions/batch`. It used to loop the single-session route, and each of those calls re-read the whole card archive server-side — 20 selected sessions were 20+ full scans for one click. Partial success is now the server's response shape (`linkedSessionIds` / `failed[]`) and the hook still reports `{ work, assigned, failed }`, so the Inbox multi-select keeps exactly the failed sessions checked for a retry and `WorksView` renders each reason on its own row (the shared banner only carries the first). The Work is only swapped into the list when **something** linked — on a total failure the server echoes the untouched record. `createWorkFromSessions` = create + that call, rolling the new Work back with `apiDeleteWork` only when nothing linked (same rule as `moveSessionToNewWork`) |
| Reopen or merge a Work | `useWorks.ts`, `useWorksApi.ts` | `reopenWork(id)` → `POST /api/works/:id/reopen`, answering the same `'done' \| 'blocked'` outcome contract as `completeWork`/`discardWork` (`409` = already active, i.e. a stale dialog) — the caller decides whether to close, and for reopen it deliberately does not. `App` chases it with `refreshCards()`: the restored cards are the whole point and the 3s board poll would only get there eventually. `mergeWork(id, intoWorkId)` → `POST /api/works/:id/merge` and applies **both** sides of the response (`to` took the links, `from` became `superseded`) — the source is kept, not deleted, so neither may be dropped from the list. `updateWorkNotes(id, notes)` is a notes-only PATCH that sends `null` for an emptied note (the server treats `''` and `null` alike) and touches no status, so a resolved Work can still be annotated |
| Clean up links whose session lost every card | `useWorks.ts`, `useWorksApi.ts` | `pruneWorkSessions` → `POST /api/works/:id/prune-sessions`, the only removal path for a link the server stamped `cardsMissingAt`. Never automatic: card deletion is a soft delete, and a card-less session does not come back to the Inbox, so `WorkDetailDialog` confirms first and shows the failure next to its own button rather than only in the shared banner |
| Change the Inbox ignore list (폐기 / 복원) | `useWorksApi.ts`, `useWorks.ts` | `ignoreSession` and `restoreIgnoredSession` are a pair — both refresh the Inbox **and** `ignoredSessions`, because the header count and the restore list must not lag the action that changed them. The list is fetched when the tab activates and after every ignore/restore, and is deliberately **not** polled: it only changes in response to something the user just did here. Undo toasts for 배정/폐기 are built by `components/Works/workAssignNotice.ts` from these same mutations called in reverse — do not add a second reversal path |
| Read the Works domain contract first | `docs/works.md` | Work/session model, status side effects, `works.*` settings keys, `/api/works*` reference |
| Change Work session move / unlink | `useWorksApi.ts`, `useWorks.ts` | `moveSession` PATCHes `/api/works/sessions/:sessionId`, applies **both** `from` and `to`, and refetches when `from` is `null` (the server deleted the emptied source). `moveSessionToNewWork` = create + move, rolling the new Work back with `deleteWork` if the move fails — there is no single server verb for it. `linkSessionToWork` takes only `Pick<WorkInboxSession,'sessionId'|'projectDir'>`, so it also serves the role change (same-Work re-link) and the undo of an unlink — do not add a second endpoint for either |
| Change Work date editing | `useWorks.ts` | `updateWorkDates` is a status-free PATCH of `startedAt`/`resolvedAt`, shared by the Timeline bar-edge drag and the Work detail dialog's date inputs — keep both on this one function. It rejects on the server's ordering/format `400` so the dialog can show it inline |
| Rename a Work / re-point its directory | `useWorks.ts` | `updateWorkMeta` — the metadata twin of `updateWorkDates`: a status-free PATCH of `title`/`projectDir`, so none of the completion side effects are in play and a resolved Work can still be renamed. Its only caller is the detail dialog's `기본 정보` row. Rejects rather than swallowing, so the dialog shows the failure next to the field |
| Change Work deletion | `useWorks.ts` | `deleteWork` = `useCrudResource.deleteEntry` + `refreshInbox()` — deleting a Work releases every linked session back to the Inbox, so the badge/list must not wait for the 10s poll. It has no status guard (a `done` Work can be deleted; its cards stay archived) and does **not** prompt: the Work detail dialog owns the status-branched `window.confirm` because it is the only entry point and already holds the Work. Delete keeps `window.confirm`; **완료/폐기 do not** — they need to show card counts and running-card state, which `window.confirm` cannot render. `moveSessionToNewWork`'s rollback calls the raw `apiDeleteWork`, so keep prompts out of this function |
| Change Work completion / discard | `useWorks.ts` | `completeWork(id, { confirmed })` sends `{ status: 'done', confirmArchive: true }` — the server bulk-archives the Work's cards, irreversibly. **Without `confirmed` it sends nothing and answers `'declined'`** while a confirmation is required, so a caller that skipped `WorkResolveConfirmDialog` gets a no-op rather than a silent mass archive. Whether one is required is the pure `requiresDoneConfirm(config)` — an **unloaded** config counts as "required", because reading `config?.doneConfirm` directly let a click that beat the config request treat `undefined` as "no prompt needed". Both this and `discardWork` return `'done' \| 'declined' \| 'blocked'` (`'blocked'` = server `409`, e.g. a card still running an agent) and the caller closes its dialog **only** on `'done'`; the old `Promise<void>` could not tell a cancelled confirmation from a completed archive, so the detail dialog closed on cancel |
| Read what a completion would destroy | `useWorkCompletionPreview.ts`, `fetchWorkCompletionPreview` in `useWorksApi.ts` | `GET /api/works/:id/completion-preview`, read **on demand** (pass `null` and nothing is fetched) because it parses archive months and consults the runtime run store, and only matters while a confirmation dialog is open. No polling; a response for a dismissed dialog is dropped via `requestRef`, same guard as `useWorkSessions`. `updateWork` returns `WorkPatchResponse` (`Work` + the `sweep` report) — a superset of `Work`, so existing callers are unaffected |
| Change scripts data flow | `useScriptsApi.ts`, `useScripts.ts` | Thin wrapper over `useCrudResource` + run/sync |
| Change Quick Actions data flow | `useQuickActionsApi.ts`, `useQuickActions.ts` | Shared CRUD wrapper + validated/idempotent execution |
| Change Quick Actions run form rules | `quickActionFormModel.ts` | Pure default merge, validation, secret-safe form model |
| Change settings data flow | `useSettingsApi.ts`, `useSettings.ts` | Thin wrapper over `useCrudResource` |
| Change question overlay data flow | `useQuestionsApi.ts`, `useQuestions.ts` | 3s polling, reply/reject |
| Change skills data flow | `useSkillsApi.ts`, `useSkills.ts` | Discovery + sync; registers dynamic skill commands via `src/core/commands.ts` |
| Change skill-roots data flow | `useSkillRootsApi.ts`, `useSkillRoots.ts` | CRUD for scanned skill root directories |
| Change scope/capabilities inventory data flow | `useScopeInventory.ts` | MCP inventory, discovered skills, context diagnostics, cold-storage manifest |
| Change scope placement-target data flow | `useScopeTargets.ts` | CRUD for `PlacementTarget`s (where scoped configs get written) |
| Change runtime/model catalog data flow | `useRuntimes.ts`, `useModelCatalog.ts`, `useRuntimeDefaults.ts` | Runtime list, model catalog merge, per-runtime default permission/sandbox settings |
| Change wiki data flow | `useWikiApi.ts` | Wiki config, ingest, query, archive-eligible cards |
| Change modal focus trapping | `useModalAccessibility.ts` | Escape + tab loop |
| Change resizable-dialog persistence | `usePersistedDialogSize.ts` | Debounced size persistence per dialog |
| Change directory-picker history | `useDirectoryHistory.ts` | `localStorage`-backed recent-directories list |
| Change font-scale preference | `useFontScale.ts` | `localStorage` + cross-component custom event |
| Change generic polling helper | `usePolling.ts` | Shared interval wrapper |
| Change alert dialog plumbing | `uiAlert.ts` | Shared `UiAlert` shape + factory used by `App.tsx` |

## CONVENTIONS

- API wrappers own raw `fetch()` and `ApiError`/`handleResponse()` handling; most target `/api` via a local `BASE_URL` constant.
- Domain hooks own reducer state, optimistic updates, refresh logic, and user-facing error strings.
- Board/questions poll continuously; scheduler/scripts/settings poll only when their tab is active.
- Hooks import shared backend types directly from `src/core/types.ts` (and `src/core/runtime-config.ts`, `src/core/commands.ts` where relevant), plus question types from `src/plugin/question-monitor.ts`.
- `useKanbanBoard.ts` keeps queue/order logic in the hook, not in board components.
- `useModalAccessibility.ts` is the shared place for focus trap + escape-close behavior. **Known gap:** its `keydown` listener is on `window` in the *bubble* phase, while `DialogSkeleton` `stopPropagation()`s every non-Escape key on `.kv2-dialog` — React forwards that to the native event, so the Tab loop never runs for a dialog rendered through `DialogSkeleton` (which is all of them). Escape still works, because that path is `DialogSkeleton`'s own handler. Anything needing keys inside a dialog registers with `capture: true` today (`BulkAssignModal`); the real fix is moving this hook to the capture phase, which touches every dialog in the app.
- Persisted UI preferences (font scale, directory history, dialog size) use `localStorage` directly inside the hook, wrapped defensively so a corrupt/missing value never throws.

## ANTI-PATTERNS

- Duplicating `ApiError`/`handleResponse()` logic inside components
- Hiding polling timers inside component trees
- Mixing domain reducers across board/scheduler/scripts/settings/capabilities instead of keeping one hook per domain
- Recreating question or modal accessibility logic ad hoc in components
- Reading/writing `localStorage` directly from a component instead of through the owning hook

## NOTES

- `useKanbanApi.ts` is broader than its name: it also exposes models, screenshots, and question endpoints used elsewhere in the app.
- Scheduler/scripts/settings hooks all share the same reducer + active-tab polling pattern; keep them consistent when extending one domain.
- Quick Actions follows the same CRUD/polling pattern; transient secret values must stay out of the returned form model and stored hook state.
- `useScopeInventory.ts` and `useScopeTargets.ts` back the Capabilities tab's scope-manager UI (inventory browsing + placement-target registration); they are newer additions and don't yet follow the exact reducer pattern of scheduler/scripts/settings — check both before assuming one is canonical.
- `useRuntimes.ts` depends on `useModelCatalog.ts` (`mergeSyncedModels`) to reconcile the static `RUNTIME_CATALOG` with server-synced models — keep that merge one-directional (server data augments, never silently drops static entries).
